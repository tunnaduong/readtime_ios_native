# Local secrets (see ReadTime/Config/Secrets.example.xcconfig)
ReadTime/Config/Secrets.xcconfig

# Xcode
xcuserdata/
DerivedData/
.DS_Store

# Swift Package Manager / Skip
.build/
.swiftpm/
.index-build/

# Android (Skip-generated Gradle project; regenerate with `skip` rather than committing it)
Android/.gradle/
Android/build/
Android/app/build/
Android/local.properties

